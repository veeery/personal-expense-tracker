import 'package:flutter/material.dart';

class ExpenseEmptyStateWidget extends StatelessWidget {
  const ExpenseEmptyStateWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
