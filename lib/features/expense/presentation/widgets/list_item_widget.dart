import 'package:flutter/material.dart';

class ExpenseListItemWidget extends StatelessWidget {
  const ExpenseListItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
